using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows;
using System.Windows.Controls;
using Utils;

namespace AvdWpfControls
{
    public class AvdProgressbar: ProgressBar
    {
        #region Dependency Properties

        #region LabelTextColor
        /*
        public static DependencyProperty ProgressCalculatorProperty =
            DependencyProperty.Register(
                "ProgressCalculator",
                typeof(ProgressCalculator),
                typeof(AvdProgressbar));
        
        public ProgressCalculator ProgressCalculator
        {
            get { return (ProgressCalculator)GetValue(ProgressCalculatorProperty); }
            set
            {
                SetValue(ProgressCalculatorProperty, value);
            }
        }
        */
        #endregion

        #endregion

        
    }
}
