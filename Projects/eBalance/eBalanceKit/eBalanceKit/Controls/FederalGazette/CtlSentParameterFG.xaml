<UserControl x:Class="eBalanceKit.Controls.FederalGazette.CtlSentParameterFG"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:my1="clr-namespace:AvdWpfControls;assembly=AvdWpfControls"
             xmlns:TemplateSelectors="clr-namespace:eBalanceKit.TemplateSelectors" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/FederalGazetteResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
  </UserControl.Resources>
  <Grid>
        <ContentPresenter Content="{Binding }" ContentTemplateSelector="{StaticResource TreeViewItemTemplateSelector}">
            <!--<ContentPresenter.-->
        </ContentPresenter>
        <!--<my1:HierarchicalTabControl
                KeyboardNavigation.TabNavigation="Contained"
				ItemsSource="{Binding Path=Items}"
				ItemTemplateSelector="{StaticResource templateFederalGazetteSelector}"
				SelectedItemChanged="navSelectionChanged">
            <! - -ItemContainerStyle="{StaticResource NavigationTreeItemContainerStyle}"- - >
        </my1:HierarchicalTabControl>-->
    </Grid>
</UserControl>
